<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android">
<!-- UNSELECTED TAB STATE -->
<item android:state_selected="false" android:state_pressed="false">
    <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
        <!-- Bottom indicator color for the UNSELECTED tab state -->
        <item android:top="-5dp" android:left="-5dp" android:right="-5dp">
            <shape android:shape="rectangle">
                <solid android:color="@color/blueapp" android:width="2dp"/>
            </shape>
        </item>
        <item android:top="-28dp" android:left="-8dp" android:right="-8dp">
            <shape android:shape="rectangle">
                <stroke android:color="@color/gray" android:width="1dp"/>
            </shape>
        </item>
    </layer-list>
</item>
<!-- SELECTED TAB STATE -->
<item android:state_selected="true" android:state_pressed="false">
    <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
        <!-- Tab background color for the SELECTED tab state -->
        <item>
            <shape>
                <solid android:color="@color/blueapp"/>
            </shape>
        </item>
        <!-- Bottom indicator color for the SELECTED tab state -->
        <item android:top="-5dp" android:left="-5dp" android:right="-5dp">
            <shape android:shape="rectangle">
                <stroke android:color="@color/blueapplight" android:width="5dp"/>
            </shape>
        </item>

        <item android:top="-28dp" android:left="-8dp" android:right="-8dp">
            <shape android:shape="rectangle">
                <stroke android:color="@color/gray" android:width="1dp"/>
            </shape>
        </item>
    </layer-list>
</item>
</selector>